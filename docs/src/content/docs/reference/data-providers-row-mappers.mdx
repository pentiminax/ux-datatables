---
title: Data Providers & Row Mappers
description: Contracts and built-in implementations for data retrieval and row mapping
---

# Data Providers & Row Mappers

## DataProviderInterface

```php
use Pentiminax\UX\DataTables\DataTableRequest\DataTableRequest;
use Pentiminax\UX\DataTables\Model\DataTableResult;

interface DataProviderInterface
{
    public function fetchData(DataTableRequest $request): DataTableResult;
}
```

Built-in providers:

<Table
  headers={['Provider', 'Use case']}
  rows={[
    ['`DoctrineDataProvider`', 'Doctrine ORM-backed datasets'],
    ['`ArrayDataProvider`', 'In-memory or preloaded arrays'],
  ]}
/>

## RowMapperInterface

```php
interface RowMapperInterface
{
    public function map(mixed $row): array;
}
```

Built-in mappers:

<Table
  headers={['Mapper', 'Use case']}
  rows={[
    ['`DefaultRowMapper`', 'Default row-to-array mapping behavior'],
    ['`ClosureRowMapper`', 'Custom mapping logic via closure'],
  ]}
/>

## DataTableResult

```php
new DataTableResult(
    recordsTotal: 1000,
    recordsFiltered: 150,
    data: $rows,
);
```

## When To Implement Custom Types

- custom domain filters with non-Doctrine backends
- APIs requiring specific output shape
- performance tuning with pre-mapped row payloads

## Cross Links

- [/ux-datatables/reference/abstract-datatable/](/ux-datatables/reference/abstract-datatable/)
- [/ux-datatables/reference/datatable-request/](/ux-datatables/reference/datatable-request/)
